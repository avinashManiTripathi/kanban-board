import React from 'react';

const Header: React.FC = () => (
  <header className="App-header">
    <div>
      <h1>Kanban Board</h1>
      <p>Organize tasks across Todo, In Progress, and Done.</p>
    </div>
  </header>
);

export default Header;

